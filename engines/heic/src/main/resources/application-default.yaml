spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
management:
  health:
    jms:
      enabled: ${MANAGEMENT_HEALTH_JMS_ENABLED:false}
queue:
  engineRequestQueue: heic-engine-queue
transform:
  core:
    version: 5.4.1
